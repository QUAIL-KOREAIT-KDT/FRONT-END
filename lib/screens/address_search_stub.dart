// 웹용 stub - kpostal을 import하지 않음

import 'package:flutter/material.dart';

/// 웹에서는 주소 검색 화면을 열지 않음 (직접 입력 사용)
Future<String?> openAddressSearch(BuildContext context) async {
  // 웹에서는 사용하지 않음
  return null;
}
